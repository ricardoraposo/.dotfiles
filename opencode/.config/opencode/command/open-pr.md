---
description: Creates a branch, commits changes atomically, and opens a pull request.
---

Opens a pull request based on a task, it could be from linear or notion.
It will create a branch, it should create multiple commits if changes have small correlations and it will open a pull request using the gh cli.
