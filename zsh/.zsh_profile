fd(){
  DIR=$(find ~/.config ~/Projects ~/Projects/Repos ~/Projects/Repos/trybe ~/Projects/Study ~/Projects/Study/trybe  -mindepth 1 -maxdepth 1 -type d | fzf) && cd $DIR
}

ntmux(){
  NAME=$(basename $PWD) && tmux new -s $NAME
}

ZSH_THEME_GIT_PROMPT_PREFIX="%{$fg[green]%}"
ZSH_THEME_GIT_PROMPT_SUFFIX="%{$reset_color%}"
ZSH_THEME_GIT_PROMPT_DIRTY=" %{$fg[red]%}✗"
ZSH_THEME_GIT_PROMPT_CLEAN=""
function prompt_char {
	if [ $UID -eq 0 ]; then echo "%{$fg[red]%}#%{$reset_color%}"; else echo $; fi
}
PROMPT='%(?, ,%{$fg[red]%}FAIL%{$reset_color%}
)
%{$fg[magenta]%}%n%{$fg[blue]%}@%{$fg[yellow]%}%m%{$reset_color%}:%{$fg_bold[blue]%}%1~%{$reset_color%}
$(prompt_char) '
# RPROMPT='$(git_prompt_info) %{$fg[blue]%}[%T]%{$reset_color%}'
RPROMPT='$(git_prompt_info)'

alias tm="ntmux"
alias tk='tmux kill-server'
alias ta='tmux attach'
alias t='npm run test'
alias d='npm run dev'
alias ls='exa -l'
alias v='nvim'
alias zf='cd $HOME/Documents/zet'
alias chmox='chmod +x'
alias sc='cd $HOME/.dotfiles/scripts'
alias dot='cd $HOME/.dotfiles'

bindkey ^E forward-word
bindkey ^B backward-word
bindkey ^K kill-word
bindkey -s ^V "v .^M"
bindkey -s ^A "t attach^M"
bindkey -s ^U "cd ~/.config/nvim^M"
bindkey -s ^F "fd^M"
